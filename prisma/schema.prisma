generator client {
  provider = "prisma-client"
  output   = "../src/lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Snippet {
  id    Int    @id @default(autoincrement())
  title String
  code  String
}
